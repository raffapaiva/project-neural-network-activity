# Dependencias do trabalho (flexiveis, instaladas e validadas na .venv local).
# Criar e instalar com:
#   python -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Versoes validadas em 24/09/2026: tensorflow 2.22.0rc0, keras 3.16.0.dev,
# numpy 2.5.3, matplotlib 3.11.2, scikit-learn 1.9.1 (Python 3.14, CPU).
# Notas: versoes diferentes de TF podem variar ~1pp de acuracia mesmo com seed fixa.

numpy>=1.26
matplotlib>=3.8
scikit-learn>=1.3
scipy>=1.11
h5py>=3.8
tensorflow>=2.16
keras>=3.0
ipykernel>=6.0
